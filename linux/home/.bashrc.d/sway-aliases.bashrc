# Sway specific
alias swayop='swaymsg -t get_outputs'
alias swayws='swaymsg -t get_workspaces'
alias swayver='swaymsg -t get_version'

# Swaync
alias swaync-rconf='swaync-client -R' # Reload swaync config
alias swaync-rcss='swaync-client -rs'   # Reload swaync CSS

